#include <iostream>
using namespace std;
int main()
{
    int num;
    int count = 0;
    int sum = 0;
    cin >> num;
    while(num!=0)
    {
        sum += num;
        count++;
        cin >> num;
    }
    cout << sum;
    return 0;
}
