#include <iostream>
using namespace std;
int main()
{
    int a, b;
    int sum = 0;
    cin >> a;
    cin >> b;
    while(a <= b)
    {
        sum += a;
        a++;
    }
    cout << sum;
    return 0;
}
